# Everything under python/hotpot — run.py and all six processes. Install
# once for the whole tree:
#
#     pip install -r python/requirements.txt
#
# websockets backs core/web/server.py's staff view (doc section 12): the
# sync flavour (websockets.sync.server / .client), not the more commonly
# documented asyncio one, because the rest of this codebase is threaded
# and the sync API matches that model directly. See that file's docstring.
#
# Lower-bounded at the version this was built and tested against (16.1),
# not pinned exactly: core/web/server.py relies on the Request/Response
# based process_request() hook, which is the part of the API most likely
# to differ across versions, so an untested older release is a real risk
# rather than a formality.
websockets>=16.1

# pyserial backs core/scale.py's load-cell thread (doc section 9.5) — the
# XIAO's `raw <c0> ... <c7>` lines at 115200. It is imported inside
# scale.py's `_open_serial()` rather than at module scope, so the parse,
# the median filter and the settle window stay importable and testable on
# a machine with neither pyserial nor a XIAO; only opening a real port
# needs this installed.
#
# Lower-bounded at the version the rig was verified against (3.5, on
# COM5, 2026-08-11). 3.5 is also where `Serial.readline()`'s timeout
# behaviour — a whole-call deadline that returns whatever arrived, which
# is what bounds that read and lets stop() be prompt — is the documented
# one.
pyserial>=3.5

# camera/main.py (M3 build item 2) uses OpenCV to pull decoded BGR frames
# from the webcam over V4L2 (`cv2.VideoCapture(..., cv2.CAP_V4L2)`) and to
# encode the MJPEG HTTP stream (`cv2.imencode`). `-headless`: this process
# never opens a window, and the non-headless wheel drags in a Qt/GTK build
# that a minimal rig image should not need. Doc section 1.4's "no AVX2"
# risk is about ML inference (MediaPipe/TFLite) leaning on it, not plain
# JPEG codec paths, so this dependency does not carry that risk — see
# camera/capture.py.
opencv-python-headless>=4.10

# core/orders.py's `qr_matrix` (M6, doc section 18.1's "a QR code
# projected"). Pure Python, no compiled extension and no Pillow needed
# for what this uses: `QRCode.get_matrix()` returns the module grid as
# nested bools, which core puts straight on the wire for oF to draw as
# filled rects (I2 — core owns the data, oF owns the pixels).
#
# Imported INSIDE `qr_matrix` rather than at module scope, the same seam
# pyserial gets above, so the order database, the receipt page and the
# whole checkout chain stay importable and testable without it. A missing
# qrcode means no QR is projected; the short order code beside it is
# still enough to pay at a counter.
qrcode>=8.0

# tracker/backend_mediapipe.py's hand tracking. Imported lazily there, so
# the tracker's own smoothing and geometry stay testable without it.
# Flagged as missing from this file on 2026-08-24 and added here: it was
# installed by hand on the dev machine, which is exactly how a rig ends up
# with a dependency nobody has written down.
mediapipe>=0.10

# docs/ROBOFLOW_PATHWAY.md — a second classifier training/deploy path
# beside Edge Impulse, additive, off `classifier.backend`. Neither of
# these two is a hard dependency of anything above: both are imported
# lazily inside backend_rf.py's classify() (same seam pyserial/mediapipe
# get above), so this whole tree — including test_backend_rf.py — stays
# importable and testable with neither installed.
#
# **DO NOT `pip install` either of these into `penv` (the interpreter
# that actually runs this app on the rig — see CLAUDE.md's 2026-08-24
# session notes) as a first move.** `inference` in particular pulls a
# large tree (torch, transformers, onnxruntime, its own numpy/opencv
# pins) that can silently downgrade `penv`'s existing numpy/opencv/
# mediapipe and break the tracker and camera process with no obvious
# connection to this feature — doc §4.3's own explicit warning. Install
# into a throwaway venv first, confirm classify() actually works there,
# THEN decide whether it can safely share `penv`.
#
# roboflow>=1.1 — classifier/rf_client.py's SDK half (labeled image
# upload, dataset version generation, Path B's weights download — see
# that module's own docstring for why these two operations use the SDK
# rather than a guessed raw-REST shape). UNVERIFIED version floor: no
# live install has been checked against this codebase yet (doc §5).
#
# inference — Path A (backend_rf.RoboflowInferenceBackend), Roboflow's
# own supported local-deployment route: `get_model()` downloads and
# caches weights on first use, later calls are offline. UNVERIFIED
# version floor, same reason as `roboflow` above; also unverified on the
# ODYSSEY deploy board — doc §4.1's own real AVX2 risk (OpenVINO's CPU
# plugin requires AVX2 from release 2026.0), unmeasurable until that
# board is in hand.
#
# onnxruntime — Path B (backend_rf.RoboflowOnnxBackend). Plain CPU
# execution provider, no AVX2 hard requirement unlike `inference`'s
# OpenVINO path (falls back to slower generic kernels instead) — doc §1's
# reason Path B stays "one config value away" rather than being dropped.
# UNVERIFIED version floor.
#
# Commented out, not installed by default: `classifier.backend` stays
# "stub" (config/system.default.json) until a Path is chosen and doc §5's
# probes have been run. Uncomment whichever Path applies once that's
# done.
# roboflow>=1.1
# inference
# onnxruntime
